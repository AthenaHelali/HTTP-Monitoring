package routes

import (
	//"github.com/AthenaHelali/HTTP-Monitoring/internal/controller"
	"github.com/labstack/echo/v4"
)

func urlRoutes(g *echo.Group) {
	//g.Use(middleware.Authentication())
	//g.POST("/url/create", controller.CreateUrl())
	//g.POST("/url/delete", controller.DeleteUrl())
	//g.GET("/url/get", controller.GetUrl())
}
